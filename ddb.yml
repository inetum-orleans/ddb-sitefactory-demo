core:
  domain:
    ext: site-factory.test
  project:
    # This line changes between branches (1/2)
    name: site1
jsonnet:
  docker:
    compose:
      # This line changes between branches (2/2)
      project_name: site1
app:
  db:
    user: 'user'
    password: 'password'
    db: 'database'