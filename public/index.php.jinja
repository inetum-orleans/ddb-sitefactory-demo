<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site2</title>
</head>
<body>
    <p>This is a totally different site.</p>
    <p>
        Here's the proof :
    </p>
    <ul>
        <li>Project: {{ core.project.name }}</li>
        <li>URL is: {{ core.domain.value }}</li>
    </ul>

    Testing connection to the database:
    <?php
    $db = new PDO('pgsql:host=db;dbname={{app.db.db}}', '{{app.db.user}}', '{{app.db.password}}');
    if ($db) {
        echo '<p>Connection successful!</p>';
    } else {
        echo '<p>Connection failed!</p>';
    }
    ?>
</body>
</html>