<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Site 1</title>
</head>
<body>
    <p>This is the website for site 1.</p>
    <p>
        Here, you can see the new configuration for the site, which is now using:
    </p>
    <ul>
        <li>Project: {{ core.project.name }}</li>
        <li>URL is: {{ core.domain.value }}</li>
    </ul>

    Testing connection to the database:
    <?php
    $db = new PDO('pgsql:host=db;dbname={{app.db.db}}', '{{app.db.user}}', '{{app.db.password}}');
    if ($db) {
        echo '<p>Connection successful!</p>';
    } else {
        echo '<p>Connection failed!</p>';
    }
    ?>
</body>
</html>